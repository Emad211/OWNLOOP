name: Finalize OL-017 Source

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  finalize:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "OL-017 chunk transfer is being assembled"
