name: Export OL-011 Review Toolchain

on:
  push:
    branches:
      - agent/ol-011-finalization-recovery

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v7

      - name: Set up exact Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.18.0

      - name: Enable Corepack and install dependencies
        shell: bash
        run: |
          set -euo pipefail
          corepack enable
          pnpm install --frozen-lockfile

      - name: Build portable review archive
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/ownloop-review-toolchain
          mkdir -p /tmp/ownloop-review-toolchain/repo /tmp/ownloop-review-toolchain/node
          rsync -a --exclude='.git' ./ /tmp/ownloop-review-toolchain/repo/
          node_root="$(dirname "$(dirname "$(readlink -f "$(command -v node)")")")"
          cp -a "$node_root"/. /tmp/ownloop-review-toolchain/node/
          tar -czf /tmp/ownloop-ol011-review-toolchain.tar.gz -C /tmp/ownloop-review-toolchain repo node
          sha256sum /tmp/ownloop-ol011-review-toolchain.tar.gz > /tmp/ownloop-ol011-review-toolchain.sha256

      - name: Upload toolchain
        uses: actions/upload-artifact@v6
        with:
          name: ownloop-ol011-review-toolchain
          path: |
            /tmp/ownloop-ol011-review-toolchain.tar.gz
            /tmp/ownloop-ol011-review-toolchain.sha256
          if-no-files-found: error
          retention-days: 1
          compression-level: 0
