diff --git a/apps/daemon/src/semantic-input/reducer.ts b/apps/daemon/src/semantic-input/reducer.ts
--- a/apps/daemon/src/semantic-input/reducer.ts	2026-07-24 11:47:55.662998196 +0000
+++ b/apps/daemon/src/semantic-input/reducer.ts	2026-07-24 11:47:55.666998196 +0000
@@ -421,6 +421,7 @@
 
 function buildCandidate(
   input: SemanticAnalysisBuilderInput,
+  graphOutcome: "complete" | "partial",
   runEvidenceId: string,
   goal: ReturnType<typeof redactSemanticGoal>,
   summaries: readonly SemanticAnalysisEvidenceSummaryV1[],
@@ -447,13 +448,13 @@
     verificationArtifactId: input.verificationArtifactId,
     verificationInputFingerprint: input.verification.inputFingerprint,
     graphContext: {
-      outcome: input.evidenceGraph.outcome,
+      outcome: graphOutcome,
       limitations: input.evidenceGraph.limitations,
       runEvidenceId,
     },
     outcome:
-      input.evidenceGraph.outcome === "complete" && !budgetTruncated ? "ready" : "partial",
-    diagnosticCode: diagnostic(input.evidenceGraph.outcome, budgetTruncated),
+      graphOutcome === "complete" && !budgetTruncated ? "ready" : "partial",
+    diagnosticCode: diagnostic(graphOutcome, budgetTruncated),
     limitations: semanticLimitations,
     inputFingerprint: inputFingerprint(input),
     goal: { evidenceId: runEvidenceId, ...goal },
@@ -501,7 +502,8 @@
   if (!(input.run.status === "Completed" || input.run.status === "Partial" || input.run.status === "Abandoned" || input.run.status === "Failed")) {
     throw new PersistenceError("operation_failed", "Semantic-input Run is not terminal.");
   }
-  if (input.evidenceGraph.outcome === "unavailable") {
+  const graphOutcome = input.evidenceGraph.outcome;
+  if (graphOutcome === "unavailable") {
     return {
       outcome: "unavailable",
       diagnosticCode: "source_unavailable",
@@ -543,6 +545,7 @@
   for (let iteration = 0; iteration < 64; iteration += 1) {
     const prepared = buildCandidate(
       input,
+      graphOutcome,
       runNode.evidenceId,
       goal,
       summaries,
diff --git a/packages/contracts/src/semantic-analysis-input.ts b/packages/contracts/src/semantic-analysis-input.ts
--- a/packages/contracts/src/semantic-analysis-input.ts	2026-07-24 11:47:55.658998196 +0000
+++ b/packages/contracts/src/semantic-analysis-input.ts	2026-07-24 11:47:55.662998196 +0000
@@ -95,7 +95,23 @@
 >;
 
 function utf8ByteLength(value: string): number {
-  return new TextEncoder().encode(value).byteLength;
+  let length = 0;
+  for (const character of value) {
+    const codePoint = character.codePointAt(0);
+    if (codePoint === undefined) {
+      continue;
+    }
+    if (codePoint <= 0x7f) {
+      length += 1;
+    } else if (codePoint <= 0x7ff) {
+      length += 2;
+    } else if (codePoint <= 0xffff) {
+      length += 3;
+    } else {
+      length += 4;
+    }
+  }
+  return length;
 }
 
 function codePointLength(value: string): number {
