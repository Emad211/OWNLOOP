onst mockedSemanticInput = vi.mocked(readValidatedRunSemanticAnalysisInputValue);
+const mockedSemanticInput = vi.mocked(readValidatedRunSemanticAnalysisInput);
 const mockedAdapter = vi.mocked(generateCandidateBatchWithResponsesAdapter);
 
 describe("Candidate generation batch processor", () => {
--- a/apps/daemon/src/candidate-generation/transport.test.ts
+++ b/apps/daemon/src/candidate-generation/transport.test.ts
@@ -66,9 +66,9 @@
     const transport = createNodeHttpsCandidateGenerationTransport({
       resolveAddresses: async () => new Promise(() => {}),
     });
-    await expect(transport(requestInput(10))).rejects.toEqual(
-      expect.objectContaining<CandidateGenerationTransportError>({ code: "timeout" }),
-    );
+    const error = await transport(requestInput(10)).catch((value: unknown) => value);
+    expect(error).toBeInstanceOf(CandidateGenerationTransportError);
+    expect((error as CandidateGenerationTransportError).code).toBe("timeout");
   });
 
   it("disables c