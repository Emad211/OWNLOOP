diff --git a/apps/daemon/src/candidate-validation/validator.ts b/apps/daemon/src/candidate-validation/validator.ts
index 2de2ce1..2886563 100644
--- a/apps/daemon/src/candidate-validation/validator.ts
+++ b/apps/daemon/src/candidate-validation/validator.ts
@@ -316,7 +316,9 @@ function factsForNodes(
 function assertions(text: string): readonly Assertion[] {
   const normalized = normalizeText(text);
   const values = new Map<string, Assertion>();
-  const add = (key: string, family: string): void => values.set(key, { key, family });
+  const add = (key: string, family: string): void => {
+    values.set(key, { key, family });
+  };
   if (/\b(?:created|added)\b/u.test(normalized)) add("change_kind:created", "change_kind");
   if (
     /\b(?:modified|updated)\b/u.test(normalized) ||
@@ -770,15 +772,21 @@ export function buildCandidateValidationReport(
   const selected = eligible.slice(0, 7);
   const selectedRank = new Map(selected.map((draft, index) => [draft.sourceIndex, index + 1]));
   const items: CandidateValidationItemV1[] = drafts.map((draft) => {
+    const citedEvidenceIds = [...draft.citedEvidenceIds];
+    const expandedEvidenceIds = [...draft.expandedEvidenceIds];
+    const facts = draft.facts.map((fact) => ({
+      ...fact,
+      evidenceIds: [...fact.evidenceIds],
+    }));
     if (draft.rejectionReasons.length > 0) {
       return {
         sourceIndex: draft.sourceIndex,
         candidateFingerprint: draft.fingerprint,
-        citedEvidenceIds: draft.citedEvidenceIds,
-        expandedEvidenceIds: draft.expandedEvidenceIds,
-        facts: draft.facts,
+        citedEvidenceIds,
+        expandedEvidenceIds,
+        facts,
         decision: "rejected",
-        reasons: draft.rejectionReasons,
+        reasons: [...draft.rejectionReasons],
         duplicateGroupId: null,
         representativeSourceIndex: null,
         attentionCost: draft.attentionCost,
@@ -791,9 +799,9 @@ export function buildCandidateValidationReport(
       return {
         sourceIndex: draft.sourceIndex,
         candidateFingerprint: draft.fingerprint,
-        citedEvidenceIds: draft.citedEvidenceIds,
-        expandedEvidenceIds: draft.expandedEvidenceIds,
-        facts: draft.facts,
+        citedEvidenceIds,
+        expandedEvidenceIds,
+        facts,
         decision: "valid_unselected",
         reasons: ["duplicate_candidate"],
         duplicateGroupId: duplicate.groupId,
@@ -808,9 +816,9 @@ export function buildCandidateValidationReport(
       return {
         sourceIndex: draft.sourceIndex,
         candidateFingerprint: draft.fingerprint,
-        citedEvidenceIds: draft.citedEvidenceIds,
-        expandedEvidenceIds: draft.expandedEvidenceIds,
-        facts: draft.facts,
+        citedEvidenceIds,
+        expandedEvidenceIds,
+        facts,
         decision: "valid_selected",
         reasons: [],
         duplicateGroupId: null,
@@ -823,9 +831,9 @@ export function buildCandidateValidationReport(
     return {
       sourceIndex: draft.sourceIndex,
       candidateFingerprint: draft.fingerprint,
-      citedEvidenceIds: draft.citedEvidenceIds,
-      expandedEvidenceIds: draft.expandedEvidenceIds,
-      facts: draft.facts,
+      citedEvidenceIds,
+      expandedEvidenceIds,
+      facts,
       decision: "valid_unselected",
       reasons: ["ranked_below_limit"],
       duplicateGroupId: null,
