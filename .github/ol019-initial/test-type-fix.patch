diff --git a/apps/daemon/src/candidate-validation/processor.integration.test.ts b/apps/daemon/src/candidate-validation/processor.integration.test.ts
index 2dd281c..7de6cea 100644
--- a/apps/daemon/src/candidate-validation/processor.integration.test.ts
+++ b/apps/daemon/src/candidate-validation/processor.integration.test.ts
@@ -36,8 +36,8 @@ import {
   generateFinalizedRunCandidateBatch,
   getCandidateGeneration,
   getRunCandidateGenerations,
-} from "./index.js";
-import { TEST_API_KEY, TEST_PROVIDER } from "./test-fixture.js";
+} from "../candidate-generation/index.js";
+import { TEST_API_KEY, TEST_PROVIDER } from "../candidate-generation/test-fixture.js";
 import { CANDIDATE_MOMENT_SCHEMA_VERSION } from "@ownloop/contracts";
 
 const HASH_A = "a".repeat(64);
diff --git a/packages/contracts/tests/candidate-validation.test.ts b/packages/contracts/tests/candidate-validation.test.ts
index 083c792..718bf1f 100644
--- a/packages/contracts/tests/candidate-validation.test.ts
+++ b/packages/contracts/tests/candidate-validation.test.ts
@@ -4,13 +4,28 @@ import {
   CandidateValidationRecordV1Schema,
   CandidateValidationReportV1Schema,
   CandidateValidationResultV1Schema,
+  type CandidateValidationReportV1,
 } from "../src/candidate-validation.js";
 
 const fingerprint = `sha256:${"a".repeat(64)}`;
 const validationId = `val_${"1".repeat(48)}`;
 const validationKey = `vkey_${"2".repeat(48)}`;
 
-function report() {
+type WidenMutable<T> = T extends string
+  ? string
+  : T extends number
+    ? number
+    : T extends boolean
+      ? boolean
+      : T extends readonly (infer Item)[]
+        ? WidenMutable<Item>[]
+        : T extends object
+          ? { -readonly [Key in keyof T]: WidenMutable<T[Key]> }
+          : T;
+
+type MutableReportFixture = WidenMutable<CandidateValidationReportV1>;
+
+function report(): MutableReportFixture {
   return {
     schemaVersion: 1,
     validatorVersion: "0.1.0",
@@ -81,7 +96,7 @@ describe("Candidate validation contracts", () => {
   });
 
   it("rejects inconsistent selected ordering and duplicate fields", () => {
-    const selected = {
+    const selected: MutableReportFixture = {
       ...report(),
       items: [
         {
@@ -220,7 +235,7 @@ describe("Candidate validation contracts", () => {
       },
       selectedRank: null,
     };
-    const value = {
+    const value: MutableReportFixture = {
       ...report(),
       items: [baseItem],
       counts: { source: 1, rejected: 0, valid: 1, selected: 0, duplicate: 0, unselected: 1 },
