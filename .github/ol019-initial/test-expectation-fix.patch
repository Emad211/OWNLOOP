diff --git a/apps/daemon/src/candidate-validation/processor.integration.test.ts b/apps/daemon/src/candidate-validation/processor.integration.test.ts
index 7de6cea..d547866 100644
--- a/apps/daemon/src/candidate-validation/processor.integration.test.ts
+++ b/apps/daemon/src/candidate-validation/processor.integration.test.ts
@@ -445,7 +445,9 @@ describe("Candidate validation processor integration", () => {
     expect(second).toEqual(first);
     expect(first).toMatchObject({
       runId: ids(RUN_A).runId,
-      outcome: "ready",
+      outcome: "partial",
+      diagnosticCode: "source_graph_partial",
+      limitations: ["source_graph_partial"],
       counts: { source: 1, rejected: 0, selected: 1 },
       reportArtifactId: expect.any(String),
       selectedSourceIndexes: [0],
diff --git a/apps/daemon/src/candidate-validation/validator.test.ts b/apps/daemon/src/candidate-validation/validator.test.ts
index 264878c..462d242 100644
--- a/apps/daemon/src/candidate-validation/validator.test.ts
+++ b/apps/daemon/src/candidate-validation/validator.test.ts
@@ -84,9 +84,10 @@ describe("Candidate validation", () => {
       validatorInput([CHANGE_CANDIDATE, duplicate, CHECK_CANDIDATE]),
     );
     expect(result.value.counts.duplicate).toBe(1);
-    expect(item(result, 1).decision).toBe("valid_unselected");
-    expect(item(result, 1).reasons).toEqual(["duplicate_candidate"]);
-    expect(item(result, 1).representativeSourceIndex).toBe(0);
+    expect(item(result, 0).decision).toBe("valid_unselected");
+    expect(item(result, 0).reasons).toEqual(["duplicate_candidate"]);
+    expect(item(result, 0).representativeSourceIndex).toBe(1);
+    expect(item(result, 1).decision).toBe("valid_selected");
     expect(item(result, 2).decision).toBe("valid_selected");
   });
 
diff --git a/apps/daemon/src/persistence/migrations.test.ts b/apps/daemon/src/persistence/migrations.test.ts
index 7214771..28fd7d8 100644
--- a/apps/daemon/src/persistence/migrations.test.ts
+++ b/apps/daemon/src/persistence/migrations.test.ts
@@ -13,6 +13,7 @@ const REQUIRED_TABLES = [
   "analysis_jobs",
   "artifacts",
   "candidate_generations",
+  "candidate_validations",
   "event_deduplication",
   "events",
   "evidence_gaps",
