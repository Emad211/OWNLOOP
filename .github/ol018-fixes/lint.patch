diff --git a/apps/daemon/src/candidate-generation/processor.ts b/apps/daemon/src/candidate-generation/processor.ts
index 9e24435..7cdf7d9 100644
--- a/apps/daemon/src/candidate-generation/processor.ts
+++ b/apps/daemon/src/candidate-generation/processor.ts
@@ -28,7 +28,7 @@ import {
   type CandidateGenerationAdapterDependencies,
 } from "./adapter.js";
 import {
-  canonicalCandidateMomentBatch,
+  type canonicalCandidateMomentBatch,
   parseCanonicalCandidateMomentBatch,
 } from "./artifact.js";
 import {
diff --git a/apps/daemon/src/candidate-generation/request.ts b/apps/daemon/src/candidate-generation/request.ts
index 45a4d2d..ab202ea 100644
--- a/apps/daemon/src/candidate-generation/request.ts
+++ b/apps/daemon/src/candidate-generation/request.ts
@@ -98,13 +98,26 @@ function validateSemanticInput(
   return semanticInput;
 }
 
+function containsControlCharacter(value: string): boolean {
+  for (const character of value) {
+    const codePoint = character.codePointAt(0);
+    if (
+      codePoint !== undefined &&
+      (codePoint <= 0x1f || (codePoint >= 0x7f && codePoint <= 0x9f))
+    ) {
+      return true;
+    }
+  }
+  return false;
+}
+
 export function validateCandidateGenerationApiKey(value: unknown): string {
   if (
     typeof value !== "string" ||
     value.length === 0 ||
     value.trim() !== value ||
     encoder.encode(value).byteLength > API_KEY_MAX_BYTES ||
-    /[\u0000-\u001f\u007f-\u009f]/u.test(value)
+    containsControlCharacter(value)
   ) {
     throw new PersistenceError("operation_failed", "The provider secret is invalid.");
   }
