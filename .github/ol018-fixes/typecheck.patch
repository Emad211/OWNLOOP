diff --git a/apps/daemon/src/candidate-generation/adapter.ts b/apps/daemon/src/candidate-generation/adapter.ts
index ea56ce5..ca78e53 100644
--- a/apps/daemon/src/candidate-generation/adapter.ts
+++ b/apps/daemon/src/candidate-generation/adapter.ts
@@ -213,7 +213,7 @@ export async function generateCandidateBatchWithResponsesAdapter(
         body: request.httpRequestBytes,
         timeoutMs: request.providerConfig.timeoutMs,
         maxResponseBytes: request.providerConfig.maxResponseBytes,
-        signal,
+        ...(signal === undefined ? {} : { signal }),
       });
       const completedAt = canonicalTimestamp(clock);
       lastRequestId = providerRequestId(response.headers["x-request-id"]);
diff --git a/apps/daemon/src/candidate-generation/transport.ts b/apps/daemon/src/candidate-generation/transport.ts
index 8f7b52e..8ae7181 100644
--- a/apps/daemon/src/candidate-generation/transport.ts
+++ b/apps/daemon/src/candidate-generation/transport.ts
@@ -1,3 +1,4 @@
+import type { LookupAddress } from "node:dns";
 import { lookup } from "node:dns/promises";
 import { request as httpsRequest } from "node:https";
 import { isIP } from "node:net";
@@ -91,7 +92,7 @@ export function createNodeHttpsCandidateGenerationTransport(): CandidateGenerati
   return async (input) => {
     if (input.signal?.aborted) throw new CandidateGenerationTransportError("aborted");
     const url = new URL(input.url);
-    let addresses: Awaited<ReturnType<typeof lookup>>;
+    let addresses: LookupAddress[];
     try {
       addresses = await lookup(url.hostname, { all: true, verbatim: true });
     } catch {
