# دفتر پیشرفت و نکات بسیار مهم

> این فایل مرجع دائمی حقیقت پروژه است.
>
> **قانون اجباری:** پیش از هر تغییر باید کامل خوانده شود و پس از هر گام معنادار با شواهد واقعی به‌روزرسانی شود.

- آخرین به‌روزرسانی: **۱۴۰۵/۰۵/۱۲ — ۲۰۲۶/۰۸/۰۳**
- شاخه: `agent/ol-028-persian-attention-loop-v0`
- پایه: `agent/ol-027-codex-adapter` در `5951ace8a125832c24855a5b63f0d32ace506445`
- آخرین کد تأییدشده پیش از این ویرایش دفتر: `67479e0a567118643fb4694e563e634ebf54d6e0`
- درخواست ادغام: `#78` — پیش‌نویس
- پروژه: **OwnLoop — لایهٔ مالکیت انسانی برای توسعهٔ نرم‌افزار با هوش مصنوعی**
- تصمیم جاری کاربر: **نصب محلی فعلاً انجام نشود؛ توسعه طبق دفتر ادامه پیدا کند.**

---

## ۱) وعدهٔ مرکزی

OwnLoop نباید فقط گزارش، log یا داشبورد عامل برنامه‌نویس باشد.

> عامل هوش مصنوعی کد را می‌نویسد؛ انسان باید فهم، تصمیم‌گیری، تشخیص ریسک و مالکیت نگهداری آن را حفظ کند.

مسئلهٔ اصلی:

- کاربر هنگام کار عامل منتظر نمی‌ماند و سراغ کار دیگری می‌رود؛
- خواندن diff و خروجی فنی خسته‌کننده است؛
- در پایان کد تحویل گرفته می‌شود، اما فهم انسان عقب می‌ماند.

چرخهٔ غیرقابل‌مذاکره:

```text
ابهام
→ پیش‌بینی یا انتخاب کاربر
→ آشکارشدن نتیجهٔ واقعی
→ شواهد قطعی
→ واکنش کوتاه
→ ثبت محلی
→ پیشرفت مالکیت انسانی
→ پایان روشن
```

این تجربه باید محدود، ۶۰ تا ۹۰ ثانیه‌ای، کم‌اصطکاک، موبایل‌پسند و بدون پیمایش بی‌پایان باشد.

---

## ۲) زیبایی و حفظ توجه

بازخورد کاربر دربارهٔ نمونهٔ مستقل نسخهٔ صفر:

> **«خیلی زشت بود.»**

این یک شکست واقعی تجربهٔ کاربری است؛ بابت آن درصد محصول بالا نرفت.

قواعد:

- رابط نباید شبیه dashboard سازمانی یا فرم فنی باشد؛
- چند ثانیهٔ اول باید حس محصول جدی و باکیفیت بدهد؛
- تایپوگرافی فارسی، ریتم، فضای تنفسی و حرکت هدفمند مهم‌اند؛
- جذابیت باید از کشف، کنترل و پیشرفت بیاید، نه امتیاز جعلی یا اعتیاد؛
- هیچ ظاهر تازه‌ای تا آزمون مستقیم کاربر «تأییدشده» نیست.

پنل AvalAI تازه از نظر ساختار و آزمون خودکار پذیرفته شده است، اما هنوز از نظر زیبایی توسط کاربر ارزیابی نشده است.

---

## ۳) مغز لحظه‌ها: AvalAI + LLM

تصمیم محصولی:

> LLM مغز پیشنهاد و قاب‌بندی Momentهاست و AvalAI واسط رسمی دسترسی OwnLoop به مدل‌هاست.

### endpointها

```text
ایران:  https://api.avalai.ir/v1
جهانی: https://api.avalai.org/v1
مسیر:  /responses
```

### مرز مسئولیت

LLM مجاز است:

- از Semantic Input کاهش‌یافته و پاک‌سازی‌شده استفاده کند؛
- عنوان، سؤال، گزینه و روایت کوتاه فارسی بسازد؛
- Momentهای مهم را پیشنهاد کند.

LLM مجاز نیست:

- Evidence ID بسازد یا تغییر دهد؛
- نتیجهٔ تست، امنیت، کامل‌بودن یا نبود ریسک را جعل کند؛
- repository، transcript یا فایل خام را مستقیم بخواند؛
- فرمان یا کد اجرایی تولید کند؛
- مرجع حقیقت باشد.

مرجع حقیقت:

```text
Git + exit code + Evidence Graph + validator قطعی
```

### امنیت کلید

- مدل hard-code نیست؛
- API key فقط در حافظهٔ daemon است؛
- در SQLite، artifact، log، diagnostics، URL یا browser storage ذخیره نمی‌شود؛
- ورودی کلید در پنل AvalAI uncontrolled است، مستقیم خوانده و بلافاصله پاک می‌شود؛
- خروجی certification شامل raw request، raw response، متن خطا یا کلید نیست؛
- کلید هرگز نباید در Chat ارسال شود.

### تصمیم زبان

- عنوان، سؤال و گزینه‌های قابل‌دیدن فارسی‌اند؛
- `claim` داخلی فعلاً کوتاه و کنترل‌شده است؛
- validator فقط facts و Evidence IDهای واقعی را می‌پذیرد؛
- متن نمایشی مدل در UI مرجع حقیقت نیست.

---

## ۴) واقعاً چقدر ساخته‌ایم؟

> درصدها تخمین مهندسی‌اند و فقط با کد، تست، اجرای واقعی یا آزمون کاربر تغییر می‌کنند.

| بخش | وضعیت واقعی | برآورد |
|---|---|---:|
| ثبت رویدادهای عامل | Claude و Codex ساخته شده؛ certification کامل Codex باز است | ۸۵٪ |
| Task Run و lifecycle | ساخته شده | ۹۰٪ |
| Git baseline و reconciliation | ساخته شده | ۹۰٪ |
| دسته‌بندی تغییرات | Evidence-backed | ۸۵٪ |
| Verification Evidence | ساخته شده | ۸۵٪ |
| Evidence Graph | ساخته شده | ۸۵٪ |
| Candidate pipeline | تولید، اعتبارسنجی، dedupe و انتخاب محدود | ۸۰٪ |
| **مغز AvalAI** | gateway، prompt، validator، پنل و harness آماده؛ live call انجام نشده | **۷۵٪** |
| grammar فارسی | unit و end-to-end ساخته شده | ۷۰٪ |
| نمایش Ownership Moment | نمای واقعی React ساخته شده | ۷۵٪ |
| ثبت تعامل | از API append-only موجود استفاده می‌کند | ۷۵٪ |
| Build Replay فنی | موجود؛ روایت کوتاه کامل نیست | ۶۵٪ |
| Windows installer/runtime | بسته، ACL، lifecycle و uninstall سبز | ۹۰٪ |
| Privacy و diagnostics | UI memory-only و summary بدون raw provider data ساخته شده | ۹۰٪ |
| **پنل فارسی AvalAI** | دامنه، مدل و کلید حافظه‌ای ساخته و تست شده؛ live call ندارد | **۸۰٪** |
| **Certification AvalAI** | compose و summary امن با transport تزریقی کامل؛ اجرای واقعی باقی است | **۸۵٪** |
| **Attention Feed شبیه ریلز** | نمای واقعی محدود ساخته شده؛ پذیرش کاربر ندارد | **۳۵٪** |
| **انتخاب → reveal → evidence → نتیجه** | در React پیاده‌سازی شده؛ تست انسانی باقی است | **۳۵٪** |
| **جلسهٔ ۶۰ تا ۹۰ ثانیه‌ای** | پایان و زمان‌سنج دارد؛ ریتم تأیید نشده | **۴۰٪** |
| Human Ownership Coverage | درصد مرور دارد؛ نقشهٔ کامل ندارد | ۱۵٪ |
| موبایل/PWA | responsive است؛ PWA و تست موبایل واقعی ندارد | ۵٪ |
| **اثبات نگه‌داشتن توجه** | آزمون رفتاری انجام نشده | **۰٪** |

جمع‌بندی:

| لایه | برآورد |
|---|---:|
| موتور شواهد و زیرساخت | ۸۰ تا ۸۵٪ |
| محصول end-to-end | ۶۰ تا ۶۳٪ |
| تجربهٔ اصلی حفظ توجه | ۳۰ تا ۳۵٪ |
| اعتبارسنجی رفتار کاربر | ۰٪ |

---

## ۵) چیزهایی که در OL-028 واقعاً ساخته شدند

### AvalAI gateway

- `apps/daemon/src/candidate-generation/avalai.ts`
- preset ایران و جهانی؛
- ساخت دقیق `/v1/responses`؛
- مدل قابل‌انتخاب؛
- عدم ورود کلید به public config؛
- `docs/guides/avalai-moment-brain.md`؛
- prompt فارسی برای متن قابل‌دیدن؛
- claim داخلی کنترل‌شده و Evidence-backed.

### validator فارسی

- `controlled-language.ts` و تست‌ها؛
- تشخیص محدود change، verification، decision، gap و partial source؛
- رد ادعاهایی مانند «هیچ ریسکی نیست» و «کاملاً امن است»؛
- Candidate فارسی سازگار با Evidence انتخاب می‌شود؛
- Candidate فارسی متناقض رد می‌شود؛
- مسیر انگلیسی قبلی حفظ شده است.

### Attention Loop واقعی React

- `apps/web/src/AttentionApp.tsx`
- `apps/web/src/attention.css`
- مسیر: `/?view=attention`
- API واقعی:

```text
listRuns → getMoments → recordMomentInteraction
```

- اسکن محدود ۱۰ اجرای اخیر؛
- توکن فقط در حافظهٔ صفحه؛
- claim داخلی انگلیسی در UI مخفی است؛
- facts قطعی فارسی می‌شوند؛
- انتخاب، reveal، ثبت، لحظهٔ بعد و summary پایان‌دار؛
- dashboard فنی قدیمی دست‌نخورده؛
- progress دسترس‌پذیر، fieldset معنایی و کلیدهای پایدار؛
- responsive و reduced-motion؛
- بدون dependency، state library یا backend جدید.

### پنل متمرکز فارسی AvalAI

- `apps/web/src/AvalAiSetupApp.tsx`
- `apps/web/src/avalai-setup.css`
- `apps/web/src/AvalAiSetupApp.test.tsx`
- مسیر: `/?view=avalai`
- فقط سه انتخاب اصلی:
  1. مسیر ایران یا جهانی؛
  2. شناسهٔ مدل؛
  3. کلید API حافظه‌ای.
- استفاده از APIهای موجود:

```text
getSettings
→ loadProviderSecret
→ updateSettings
→ clearProviderSecret
```

- حفظ retention، diagnostic mode و secret-field patterns موجود؛
- فعال‌سازی `responses_json_v1` بدون provider registry؛
- پاک‌شدن مقدار input کلید بلافاصله پس از خواندن؛
- عدم استفاده از `localStorage` و `sessionStorage`؛
- نمایش مرز اعتماد: LLM پیشنهاد، validator کنترل، Git/Evidence حقیقت؛
- حالت‌های conflict، unauthorized، invalid model و clear secret؛
- responsive و reduced-motion؛
- بدون endpoint، backend، dependency یا state library تازه.

### Certification harness امن AvalAI

- `apps/daemon/src/candidate-generation/avalai-certification.ts`
- `apps/daemon/src/candidate-generation/avalai-certification.test.ts`
- export از `candidate-generation/index.ts`؛
- compose مستقیم مرزهای موجود:

```text
AvalAI provider options
→ prepareCandidateGenerationRequest
→ generateCandidateBatchWithResponsesAdapter
→ privacy-safe certification summary
```

- transport تزریقی؛
- بدون endpoint، persistence، queue یا network stack تازه؛
- خروجی محدود شامل:
  - region؛
  - status و diagnostic code؛
  - request و provider fingerprints؛
  - provider request ID امن؛
  - تعداد Candidate؛
  - fingerprint دستهٔ Candidate؛
  - usage؛
  - خلاصهٔ attemptها.
- خروجی شامل API key، Authorization header، request body، provider body، raw error یا متن پاسخ نیست؛
- سناریوهای آزموده‌شده:
  - پاسخ strict موفق از endpoint ایران؛
  - endpoint جهانی؛
  - JSON envelope نامعتبر؛
  - timeout؛
  - response بزرگ‌تر از حد؛
  - عدم نشت secret یا raw provider content.

---

## ۶) شواهد نهایی

### Attention Loop و AvalAI gateway

```text
CI #1029
commit: ed419e1e6d278675a74092b2a483f2a716eb7a7b
run: 30773008227
```

هر دو job `verify` و `windows-package-smoke` موفق شدند.

### پنل فارسی AvalAI

```text
CI #1039
commit: a353a51a34c56bab15ecf3724442eb6da11f3145
run: 30774351354
```

هر دو job `verify` و `windows-package-smoke` موفق شدند.

### Certification harness AvalAI

```text
CI #1046
commit: 67479e0a567118643fb4694e563e634ebf54d6e0
run: 30774798558
```

#### job verify — موفق

- frozen install؛
- format؛
- lint؛
- typecheck؛
- تمام تست‌های مخزن؛
- چهار تست تازهٔ certification؛
- build.

#### job windows-package-smoke — موفق

- ساخت بستهٔ verified Windows؛
- ACL خصوصی؛
- محیط نصب جدا؛
- نصب و daemon preflight؛
- start/status/stop؛
- Hook repair flows؛
- preserve-data uninstall؛
- reinstall؛
- remove-data uninstall؛
- حفظ تنظیمات و Hookهای نامرتبط.

### هنوز شواهد نداریم

- live call واقعی AvalAI؛
- strict Candidate JSON واقعی از AvalAI؛
- Moment فارسی تولیدشده توسط AvalAI در React؛
- اجرای نصب‌شدهٔ نمای تازه روی دادهٔ واقعی کاربر؛
- تأیید زیبایی و ریتم توسط کاربر؛
- اثبات کاهش ترک‌کردن صفحه.

---

## ۷) اصول تصمیم‌گیری

### باید انجام دهیم

۱. هر برش در کمتر از چند دقیقه قابل‌دیدن باشد.  
۲. از API، قرارداد و persistence موجود استفاده شود.  
۳. تعداد Momentها محدود و پایان روشن باشد.  
۴. شواهد فقط پس از انتخاب آشکار شوند.  
۵. LLM پیشنهاد دهد و validator تصمیم بگیرد.  
۶. AvalAI واسط رسمی مدل‌ها باشد.  
۷. کلید فقط در حافظه بماند.  
۸. پیش از هر تغییر این دفتر خوانده شود.  
۹. پس از هر گام این دفتر به‌روزرسانی شود.

### فعلاً نباید انجام دهیم

- اتصال به عامل تازه؛
- سرویس، queue، worker یا provider registry جدید؛
- cloud state، حساب کاربری، پرداخت یا telemetry؛
- marketplace مدل یا fallback پیچیده؛
- بازنویسی کامل Settings یا dashboard؛
- کتابخانهٔ state یا animation جدید؛
- امتیاز جعلی، streak یا infinite scroll؛
- ادعای فهم فقط بر اساس کلیک؛
- ذخیره یا انتقال کلید در Chat؛
- ذخیرهٔ raw provider payload برای certification؛
- افزایش درصد بدون اجرای واقعی.

---

## ۸) گام جاری

> مرز کدنویسی certification بسته شده است. live call و آزمون انسانی به محیط محلی نیاز دارند و تا تصمیم بعدی کاربر به تعویق می‌مانند.

### انجام‌شده

- [x] شکست بصری نمونهٔ صفر ثبت شد.
- [x] AvalAI رسمی شد.
- [x] preset ایران/جهانی ساخته شد.
- [x] کلید memory-only ماند.
- [x] prompt فارسی و claim کنترل‌شده ساخته شد.
- [x] grammar فارسی و تست end-to-end ساخته شد.
- [x] نمای React واقعی ساخته شد.
- [x] interaction به API واقعی وصل شد.
- [x] پنل کوچک فارسی AvalAI ساخته شد.
- [x] تنظیمات نامرتبط هنگام فعال‌سازی AvalAI حفظ می‌شوند.
- [x] certification harness بدون endpoint و persistence تازه ساخته شد.
- [x] موفقیت، JSON نامعتبر، timeout، response بزرگ و عدم نشت secret آزموده شد.
- [x] failureهای provider به summary کنترل‌شده تبدیل می‌شوند.
- [x] CI کامل Linux/Windows برای harness سبز شد.

### به تعویق کاربر / نیازمند محیط محلی

- [ ] بستهٔ تازه روی سیستم کاربر نصب شود؛ بدون حذف داده.
- [ ] `/?view=attention` با Run واقعی اجرا شود.
- [ ] `/?view=avalai` با کلید حافظه‌ای واقعی اجرا شود.
- [ ] live call AvalAI اجرا شود.
- [ ] strict Candidate JSON واقعی دریافت و parse شود.
- [ ] اولین Moment فارسیِ تولیدشده از AvalAI نمایش داده شود.
- [ ] کاربر زیبایی، وضوح و ریتم را ارزیابی کند.

### باقی‌ماندهٔ فنی بدون نصب

- [ ] بررسی اینکه آیا برای اجرای live certification یک entrypoint محدودِ توسعه‌ای لازم است یا مسیر production موجود کافی است؛ پیش‌فرض: چیزی اضافه نشود مگر نیاز واقعی ثابت شود.
- [ ] انتخاب گام محصولی بعدی مستقل از نصب فقط با جلوگیری از بیش‌مهندسی.

---

## ۹) ترتیب بعدی

تا زمانی که نصب به تعویق افتاده است:

۱. از افزودن endpoint، CLI یا ابزار توسعه‌ای تازه بدون نیاز اثبات‌شده خودداری شود؛  
۲. PR #78 و دفتر با شواهد فعلی هم‌تراز بمانند؛  
۳. گام بعدی باید یک ارزش محصولی مستقل و قابل‌آزمون بدون نصب داشته باشد؛ در غیر این صورت این checkpoint باز نگه داشته شود.

پس از اجازهٔ کاربر برای نصب:

۴. `uninstall --preserve-data` نسخهٔ نصب‌شدهٔ قدیمی؛  
۵. build و install بستهٔ branch فعلی؛  
۶. بازگرداندن حالت Codex-only Hook؛  
۷. ورود توکن فقط در UI محلی؛  
۸. ورود کلید AvalAI فقط در `/?view=avalai`؛  
۹. اجرای certification واقعی و بررسی summary بدون secret؛  
۱۰. مشاهدهٔ Moment فارسی و ثبت بازخورد کاربر.

---

## ۱۰) دفتر تغییرات

| تاریخ | گام | نتیجهٔ واقعی | شواهد | بعدی |
|---|---|---|---|---|
| ۲۰۲۶/۰۸/۰۳ | خط مبنا | وعده و درصدها ثبت شد | مرور ریپو | چرخهٔ توجه |
| ۲۰۲۶/۰۸/۰۳ | نمونهٔ صفر | منطق مستقل ساخته شد | HTML مستقل | آزمون کاربر |
| ۲۰۲۶/۰۸/۰۳ | آزمون انسانی | ظاهر رد شد | بازخورد «خیلی زشت» | کپی‌نکردن ظاهر |
| ۲۰۲۶/۰۸/۰۳ | AvalAI gateway | gateway رسمی و preset ساخته شد | کد و تست | live call |
| ۲۰۲۶/۰۸/۰۳ | validator فارسی | grammar و E2E سبز | CI | React واقعی |
| ۲۰۲۶/۰۸/۰۳ | React Attention Loop | داده و interaction واقعی وصل شد | CI #1029 | نصب محلی و آزمون کاربر |
| ۲۰۲۶/۰۸/۰۳ | پنل فارسی AvalAI | دامنه، مدل و کلید memory-only با حفظ تنظیمات ساخته شد | CI #1039 | certification harness |
| ۲۰۲۶/۰۸/۰۳ | تصمیم کاربر | نصب محلی فعلاً به تعویق افتاد | پیام مستقیم کاربر | ادامهٔ فنی بدون نصب |
| ۲۰۲۶/۰۸/۰۳ | Certification AvalAI | summary امن و failure mapping بدون raw payload ساخته شد | CI #1046 | live call محلی |

---

## ۱۱) نکاتی که نباید فراموش شوند

۱. **OwnLoop داشبورد عامل نیست؛ لایهٔ مالکیت انسانی است.**  
۲. **موتور شواهد وسیله است، نه محصول نهایی.**  
۳. **محصول وقتی موفق است که انسان واقعاً توجه کند و بفهمد.**  
۴. **ریلز یعنی اصطکاک کم و ریتم سریع؛ نه اعتیاد و بی‌نهایت.**  
۵. **پایان محدود بخشی از محصول است.**  
۶. **کلیک مساوی فهم یا مالکیت نیست.**  
۷. **Git و Evidence مرجع‌اند؛ متن مدل مرجع نیست.**  
۸. **AvalAI واسط رسمی LLM است.**  
۹. **کلید AvalAI هرگز در Chat یا durable storage قرار نمی‌گیرد.**  
۱۰. **ظاهر ضعیف شکست محصولی است.**  
۱۱. **نسخهٔ تازه تا آزمون کاربر «زیبا» نامیده نمی‌شود.**  
۱۲. **سرعت مهم است و بیش‌مهندسی ممنوع است.**  
۱۳. **قبل از هر تغییر دفتر خوانده شود.**  
۱۴. **پس از هر گام دفتر به‌روزرسانی شود.**