export const GIT_RECONCILIATION_COLLECTOR_VERSION = "0.1.0" as const;
export const GIT_RECONCILIATION_PATH_IDENTITY_VERSION = "ownloop-reconciliation-path-v1" as const;
export const GIT_RECONCILIATION_EVENT_DEDUPLICATION_VERSION = "v1:git-reconciliation" as const;

export const DEFAULT_GIT_RECONCILIATION_ENTRY_LIMIT = 2_000;
export const MAX_GIT_RECONCILIATION_BATCH = 25;
